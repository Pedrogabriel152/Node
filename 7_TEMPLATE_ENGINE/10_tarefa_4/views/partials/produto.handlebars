<div class="produtos">
    <div class="produto">
        <h1>{{this.titulo}}</h1>
        <p>{{this.descricao}}</p>
        <h3>Valor: R$ {{this.valor}}.00</h3>
        <a href="/produtos/{{this.id}}">Veja mais</a>
    </div>
</div>